{
  "scripts": {
    "format": "prettier --ignore-path .gitignore --write .",
    "types": "tsc",
    "test": "tsc --watch"
  },
  "private": true,
  "dependencies": {
    "prettier": "^2.6.2",
    "typescript": "^4.6.3"
  }
}
